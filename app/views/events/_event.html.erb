<li class="event" >
  <%= link_to "#" do %>
    <%= image_tag event.image_path, class: "event-image" %>
    <div class="label">
      <h1><%= event.title %></h1>
      <h2><%= event.start_date.strftime("%d.") %> - <%= event.end_date.strftime("%d. %B %Y") %></h2>
    </div>
  <% end %>
</li>
